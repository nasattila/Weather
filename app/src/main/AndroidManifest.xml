<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.aut.weather">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="hu.bme.aut.weather.MainApplication"
        android:allowBackup="true"
        android:icon="@drawable/weather_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/weather_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBpfOlsmG31ZgZIGL3OlwUv96K44HIlZFA"/>

        <!-- <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" /> -->

        <activity
            android:name="hu.bme.aut.weather.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="hu.bme.aut.weather.ViewWeatherActivity"></activity>
        
    </application>

</manifest>